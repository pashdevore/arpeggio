json.extract! @song, :id, :user_id,
                     :title,
                     :description,
                     :image_url, :thumbnail_url, :image_width, :image_height,
                     :audio_file_name,
                     :created_at, :updated_at
